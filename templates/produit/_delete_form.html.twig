<form method="post" action="{{ path('app_produit_delete', {'ref': produit.ref}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit.ref) }}">
    <button class="btn">Delete</button>
</form>
