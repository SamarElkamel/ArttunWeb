<form method="post" action="{{ path('app_livreur_delete', {'id': id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>